import FetchPokemon from "./Pokemons/FetchPokemon";

const Pokemons = () => {
	return (
		// className inserted for background and center.
		<div className="App | App-header">
			<FetchPokemon />
		</div>
	);
};

export default Pokemons;
